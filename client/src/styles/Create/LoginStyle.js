import styled from 'styled-components';

const LoginStyle = styled.div`
  width: 20%;
  margin-top: 20px;
`;

const LoginContainer = styled.div`
  .loginTitle {
    display: flex;
    justify-content: center;
  }
`;

const LoginForm = styled.div`
  .loginFormStyling {
    color: black;
  }
`;

export { LoginStyle, LoginContainer, LoginForm };
